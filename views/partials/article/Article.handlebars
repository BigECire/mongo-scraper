<div class="card">
    <h5 class="card-header">{{Headline}}</h5>
    <div class="card-body">
        <div class="container">
            <div class="row">
                <div class="col-sm-11">
                </div>
                <div class="col-sm-1">
                    <button type="button" class="btn btn-primary">Add To Saved</button>
                </div>
            </div>
            <div class="row">
                <p class="card-text">{{summary}}</p>
                <a href="{{url}}" class="btn btn-primary">Link To The Original Article</a>
            </div>
        </div>
    </div>
</div>